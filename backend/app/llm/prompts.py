"""Все промпты в одном месте — критично для итерации (см. ТЗ 6.4, 13).

Меняем формулировки здесь, не трогая pipeline. Под OpenAI json_object: верхний
уровень ответа всегда ОБЪЕКТ (массивы заворачиваем в ключ).
"""

# --- Этап 1. Extract ---------------------------------------------------------

EXTRACT_SYSTEM = (
    "Ты помогаешь продавцу на маркетплейсе анализировать отзывы конкурента. "
    "Ты выделяешь из отзывов конкретные претензии и похвалы. "
    "Отвечай ТОЛЬКО валидным JSON-объектом, без markdown-обрамления и пояснений."
)

EXTRACT_USER_TEMPLATE = """Вот {n} отзывов о товаре. Для каждого отзыва выдели:
- claims_negative: список конкретных жалоб (каждая 3-7 слов, по сути проблемы)
- claims_positive: список конкретных похвал (каждая 3-7 слов)
- emotional_tone: одно из "positive" / "neutral" / "negative" / "mixed"

Если в отзыве нет жалоб или похвал — верни пустой список для соответствующего поля.

Верни строго JSON-объект вида:
{{"reviews": [{{"claims_negative": [...], "claims_positive": [...], "emotional_tone": "..."}}]}}
Массив reviews — в том же порядке, что и отзывы.

Отзывы:
{reviews_block}"""


# --- Этап 2. Cluster ---------------------------------------------------------

CLUSTER_SYSTEM = (
    "Ты кластеризуешь короткие фразы-претензии (или похвалы) покупателей в темы. "
    "Отвечай ТОЛЬКО валидным JSON-объектом, без markdown и пояснений."
)

CLUSTER_NEGATIVE_TEMPLATE = """Вот список претензий покупателей к товару. Кластеризуй похожие в группы.
Для каждой группы укажи:
- topic: короткое название темы (3-5 слов)
- frequency: сколько фраз попало в группу (целое число)
- severity: "high" / "medium" / "low" — насколько критична проблема для продаж
- sample_quotes: 3 наиболее характерные фразы из группы

Оставь только группы с frequency >= 3. Отсортируй по убыванию frequency.
Верни строго JSON-объект вида: {{"clusters": [{{...}}]}}.

Претензии:
{claims_block}"""

CLUSTER_POSITIVE_TEMPLATE = """Вот список похвал покупателей товару. Кластеризуй похожие в группы.
Для каждой группы укажи:
- topic: короткое название темы (3-5 слов)
- frequency: сколько фраз попало в группу (целое число)
- sample_quotes: 3 наиболее характерные фразы из группы

Оставь только группы с frequency >= 3. Отсортируй по убыванию frequency.
Верни строго JSON-объект вида: {{"clusters": [{{...}}]}}.

Похвалы:
{claims_block}"""


# --- Этап 3. Synthesize ------------------------------------------------------

SYNTH_SYSTEM = (
    "Ты — опытный аналитик маркетплейсов, который помогает селлеру понять "
    "сильные и слабые стороны товара конкурента и придумать, как сделать лучше. "
    "Пиши простым языком, понятным селлеру без аналитической подготовки. "
    "Отвечай ТОЛЬКО валидным JSON-объектом строго по заданной схеме, без markdown и пояснений."
)

SYNTH_USER_TEMPLATE = """На основе данных по товару конкурента и кластеров отзывов составь итоговый отчёт.

КАРТОЧКА ТОВАРА:
- Название: {title}
- Бренд: {brand}
- Рейтинг: {rating}
- Проанализировано отзывов: {reviews_analyzed}

КЛАСТЕРЫ ЖАЛОБ (topic / frequency / severity / примеры):
{complaints_block}

КЛАСТЕРЫ ПОХВАЛ (topic / frequency / примеры):
{praises_block}

Составь JSON-объект ровно такой структуры:
{{
  "summary": "Краткое резюме одним абзацем на 3-4 предложения: что в целом с товаром, главные боли и сильные стороны",
  "product_info": {{"title": "...", "brand": "...", "rating": число_или_null, "reviews_analyzed": {reviews_analyzed}}},
  "complaints": [
    {{"topic": "...", "frequency": число, "severity": "high|medium|low", "description": "1-2 предложения что именно не так", "sample_quotes": ["...", "..."]}}
  ],
  "praises": [
    {{"topic": "...", "frequency": число, "description": "1-2 предложения за что хвалят", "sample_quotes": ["...", "..."]}}
  ],
  "opportunities": [
    {{"category": "product|card|infographic|description", "title": "Короткая рекомендация", "rationale": "Почему это сработает, со ссылкой на данные отзывов"}}
  ],
  "demographic_hints": "1-2 предложения о том, какая аудитория прослеживается по отзывам (если видно)"
}}

Правила:
- complaints и praises отсортируй по убыванию frequency, максимум по 7 пунктов.
- opportunities: 4-7 конкретных идей, привязанных к данным. Покрой категории product (сам товар), card (карточка/фото), infographic (инфографика), description (описание).
- Каждая рекомендация должна опираться на конкретную жалобу или похвалу.
- Никакой воды и общих фраз. Только то, что селлер может применить.
"""
