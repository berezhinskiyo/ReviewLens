# Caddy с автоматическим HTTPS (Let's Encrypt).
# Единый домен: /api/* → backend, остальное → статика фронта (nginx).
# Замените домен на свой перед деплоем.

reviewlens.ru, www.reviewlens.ru {
	@api path /api/*
	reverse_proxy @api backend:8000

	reverse_proxy frontend:80
}
